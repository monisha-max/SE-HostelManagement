// lib/models/session.dart

class Session {
  static String loginId = '';
  static String role    = '';   // e.g. "Student", "Warden", etc.
  static int    userId  = 0;    // your logged-in User.id
  static int    roleId  = 0;    // your logged-in User.role.id
}
